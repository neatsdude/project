@DB_Server 
	edit /etc/mysql/my.cnf
	> #bind-address           = 127.0.0.1
	  bind-address            = 0.0.0.0

	Execute following commands
	$mysql -u root --host=127.0.0.1 -p

	mysql>use mysql

	mysql>GRANT ALL ON *.* to root@'%' IDENTIFIED BY 'password';

	mysql>FLUSH PRIVILEGES;

	mysql> SELECT host FROM mysql.user WHERE User = 'root';


# http://support.moonpoint.com/languages/python/authentication-insecure-password.php
Enter password:
Welcome to the MariaDB monitor.  Commands end with ; or \g.
Your MariaDB connection id is 2822
Server version: 5.5.41-MariaDB MariaDB Server

Copyright (c) 2000, 2014, Oracle, MariaDB Corporation Ab and others.

Type 'help;' or '\h' for help. Type '\c' to clear the current input statement.

MariaDB [(none)]> SET old_passwords=0;
Query OK, 0 rows affected (0.09 sec)

MariaDB [(none)]> set password=PASSWORD('2Access-Info2');
Query OK, 0 rows affected (0.08 sec)

MariaDB [(none)]> exit;
Bye

	> Restart mysql service #iff required

@python / make python file
	import MySQLdb as mdb
	db = mdb.connect(host= '<ip of mysql>', user = 'root', passwd = '<password for root>', database = '<db_name>')
	cursor = db.cursor()
	querry = "<any mysql querry>"
	cursor.execute(querry)	#gives no. of lines as output
	data = cursor.fetchall()	#saves the result of querry tuple format
	print(data)	#output in tuples format
	data = list(data)	#converts tuples into lists
	for i in data:
		print(i)
	#prints line by line output
